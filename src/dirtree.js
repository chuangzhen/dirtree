#!/usr/bin/env node

const fs = require('fs')
const path = require('path')
var args = process.argv.splice(2)
var args0 = process.argv.splice(0)
var args1 = process.argv.splice(1)
// var generation_type = process.argv.splice(3)
console.log("🚀 ~ file: dirtree.js ~ line 6 ~ args", JSON.stringify(args))
function getFolder(dir) {
     
}